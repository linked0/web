# Rust Project

## Set up environment for the first time
```
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
rustc --version
cargo --version
cargo new exr
```

Compile
```
cargo build
```

Run
```
cargo run
```