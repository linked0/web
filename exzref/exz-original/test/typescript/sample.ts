import { expect } from "chai";
import { ethers } from "hardhat";

describe("MyToken", function () {
  it("Should log test result", async function () {
    Array.from({ length: 5 }, (i, j) => j).map((i, j) => console.log(i, j));
  });
});
