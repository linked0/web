  let JAY: string = "Jay";
  let DATA: string = "Data";
  const paymaster = hexConcat([
    JAY, hexZeroPad(hexlify("0x100", { hexPad: 'left' }), 16),
    hexZeroPad(hexlify("0x200", { hexPad: 'left' }), 16), DATA
  ])
  console.log("gasLimit: ", gasLimit)
  console.log("paymaster: ", paymaster)

       Error: invalid hexlify value (argument="value", value="Jay", code=INVALID_ARGUMENT, version=bytes/5.7.0)

       : Why does the error happen and how can I solve it.